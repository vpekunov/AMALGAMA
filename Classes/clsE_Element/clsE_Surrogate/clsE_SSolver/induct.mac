@versions(Auto)
@context(PREV,infinity):-
  (((^)|(\.)+)(\s*\\n)*)\s*.
@glue:-.
@global_unique(MAIN,infinity):-
   (([^\.]*\.)->{Grammar.ru{SENTENCE}}(*PRUNE))?=>{
      xpathf(SENTENCE,'MVv1ii','Решим','систему','true')
   }.
 @auto:-"ID239" => "ID".
 @goal:-assertz(e_ssolver('ID239')),!.
 @done:-clear_db.

